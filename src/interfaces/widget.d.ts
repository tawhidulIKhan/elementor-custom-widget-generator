export interface Section {
  id: string;
  label: string;
  tab: string;
  controls: any;
}

export interface SectionInterface {
  id: string;
  label: string;
  tab: string;
  controls: any;
}

export interface ControlInterface {
  id: string;
  label: string;
  type: string;
}
